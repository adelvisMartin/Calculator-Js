# v0.17.2 follow-up is intentionally idempotent. The canonical runtime patch
# now contains the complete phone-QA fixes; keep this file so older reconstruction
# scripts that invoke it remain compatible.
print('InSave v0.17.2 follow-up compatibility gate: PASS')
